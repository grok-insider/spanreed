// Generated by fabrials-model; do not edit.
// JSON timestamps use JavaScript numbers, not bigint.

export type HistorySample = { ts_ms: number, provider: string, plan?: string | null, label: string, used: number, limit: number, resets_at?: string | null, window_start_ms?: number | null, limit_window_secs?: number | null, kind: string, event?: string | null, };

export type UsageRecord = { ts_ms: number, session_id?: string | null, model?: string | null, input_tokens: number, output_tokens: number, cached_input_tokens: number, reasoning_tokens: number, total_tokens: number, 
/**
 * xAI `cost_in_usd_ticks` (1e9 ticks = $1). Zero when not provided.
 */
cost_usd_ticks: number, request_id?: string | null, 
/**
 * Host identity that paid this hop (`grok/heavy`). Missing on old lines.
 */
account_id?: string | null, 
/**
 * Fabric route: `grok` (cli-chat-proxy) or `xai` (api.x.ai).
 */
route?: string | null, 
/**
 * Upstream provider id when not implied by `route` (`codex`, `claude`, …).
 */
provider?: string | null, 
/**
 * SHA-256 hex of the proxy Bearer that authorized this hop. Never the plaintext key.
 */
key_hash?: string | null, 
/**
 * `chat`, `stt`, `tts`, `realtime`, `image`, `video`, `models`.
 */
kind?: string | null, 
/**
 * Wall time of the hop (WebSocket session or HTTP round-trip). Never audio length.
 */
duration_ms?: number | null, 
/**
 * HTTP status or 101 after a WebSocket upgrade. None on legacy rows.
 */
status?: number | null, 
/**
 * Billing unit: `tokens`, `chars`, `audio_ms`, `images`, `video_ms`.
 */
unit?: string | null, 
/**
 * Amount in `unit` (character count, audio ms, image count, video ms).
 */
quantity?: number | null, };

export type AccountScope = { environment: string, owner: string, provider: string, account: string, };

export type Availability = "available" | "unsupported" | "unavailable";

export type Freshness = "fresh" | "stale";

export type Observation<T> = { availability: Availability, freshness: Freshness, observedAtMs: number | null, source: string, value: T | null, error: string | null, };

export type ResetCredit = { validFromMs: number | null, expiresAtMs: number | null, };

export type ResetInventory = { available: number, credits: Array<ResetCredit>, detailsComplete: boolean, };

export type CreditBalance = { remainingUsd: number | null, subscriptionRemainingUsd: number | null, subscriptionLimitUsd: number | null, purchasedRemainingUsd: number | null, paidAccess: boolean | null, };

export type Capabilities = { detection: boolean, oauth: boolean, apiKey: boolean, quota: boolean, balance: boolean, resetInventory: boolean, models: boolean, inference: boolean, };

export type ProviderDescriptor = { id: string, name: string, capabilities: Capabilities, };

export type SharingConsent = { shareMetrics: boolean, syncHistory: boolean, };

export type CredentialKind = "apiKey" | "oauth" | "unavailable";

export type MigrationCandidate = { id: string, provider: string, alias: string, generation: string, credentialKind: CredentialKind, };

export type MigrationSelection = { sourceId: string, targetAlias: string, };

export type MigrationAction = "copyApiKey" | "authorizeOAuth";

export type MigrationItem = { sourceId: string, sourceGeneration: string, provider: string, targetAlias: string, action: MigrationAction, };

export type MigrationReview = { sourceEnvironment: string, destinationEnvironment: string, items: Array<MigrationItem>, };

export type MigrationDirection = "localToHosted" | "hostedToLocal";

export type MigrationInvitation = { id: string, secret: string, expiresAtMs: number, };

export type MigrationSessionView = { id: string, ownerId: string, direction: MigrationDirection, hostedEnvironment: string, localEnvironment: string | null, phase: string, review: MigrationReview | null, revision: string | null, expiresAtMs: number, };

export type MetricKind = "quota" | "plan" | "cost" | "models" | "cache" | "trend" | "error" | "other";

export type ProgressFormat = { "kind": "percent" } | { "kind": "dollars" } | { "kind": "count", suffix: string, };

export type BarChartPoint = { label: string, value: number, valueLabel?: string, };

export type MetricLine = { "type": "text", kind: MetricKind, label: string, value: string, color?: string, subtitle?: string, } | { "type": "progress", kind: MetricKind, label: string, used: number, limit: number, format: ProgressFormat, resetsAt?: string, color?: string, } | { "type": "badge", kind: MetricKind, label: string, text: string, color?: string, subtitle?: string, } | { "type": "barChart", kind: MetricKind, label: string, points: Array<BarChartPoint>, note?: string, color?: string, };

export type ProviderOutput = { resetInventory?: Observation<ResetInventory>, providerId: string, displayName: string, plan?: string, lines: Array<MetricLine>, };

# fabrials-metrics

Pricing table, public list-price cost for `UsageRecord`, JSONL ledger I/O, and
SSE usage parsing.

```toml
fabrials-metrics = "0.1.0"
```

crates.io. Depends on `fabrials-model` 0.1.

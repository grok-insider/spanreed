# fabrials-core

Portable contracts and policies used by Spanreed and ai-relay. This crate contains no I/O or UI code. Provider observations distinguish unavailable data from zero, and subscription balances from purchased credits. Credential recovery never serializes secrets into public observations.

Run `cargo test` to verify domain invariants.

# fabrials-share

Build `ShareSnapshot` JSON for fabrials.com. No HTTP — callers POST.

`source.app` is `spanreed` or `ai-relay`.

- `snapshot_from_outputs` — probe lines (CLI).
- `economics_from_hops` / `snapshot_from_hops` — hops + pool % (hosted relay).

```toml
fabrials-share = "0.2.0"
```

# fabrials-oauth-grok

Parse Grok CLI auth blobs and the headers the capture fabric injects upstream.
Device-code HTTP stays in the binary (injectable client).

```toml
fabrials-oauth-grok = "0.1.1"
```

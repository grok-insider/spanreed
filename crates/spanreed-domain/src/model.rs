//! Probe/output DTOs. Owned by [`fabrials-types`]; re-exported so `crate::model::…` stays stable.

pub use fabrials_types::{
    BarChartPoint, MetricKind, MetricLine, ProbeView, ProgressFormat, ProviderOutput,
};

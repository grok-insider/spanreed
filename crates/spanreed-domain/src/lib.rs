//! Spanreed domain: the provider output model and pure logic (formatting,
//! usage statistics, tray presentation rules) plus the ports that the
//! application implements. No I/O.

pub mod model;
pub mod output;
pub mod ports;
pub mod provider_icons;
pub mod tray_format;
pub mod usage_stats;
pub mod util;
